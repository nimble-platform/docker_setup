version: '3'

services:
  ##########################
  #### MS Infrastructure ###
  ##########################

  config-server:
    image: nimbleplatform/config-server:latest
    env_file:
      - env_vars
    ports:
      - "8888:8888"

  service-discovery:
    image: nimbleplatform/service-discovery:latest
    env_file:
      - env_vars
    ports:
      - "8761:8761"
    depends_on:
      - config-server

  gateway-proxy:
    image: nimbleplatform/gateway-proxy:latest
    env_file:
      - env_vars
    ports:
      - "443:80"
    depends_on:
      - config-server
      - service-discovery

  hystrix-dashboard:
    image: nimbleplatform/hystrix-dashboard:latest
    env_file:
      - env_vars
    ports:
      - "9080:9080"
    depends_on:
      - config-server
      - service-discovery

  ##########################
  ######## Keycloak ########
  ##########################

  # keycloak:
  #   image: jboss/keycloak-postgres:3.2.0.Final
  #   env_file:
  #     - keycloak_default_env
  #     - keycloak_secrets
  #   links:
  #     - keycloak-db:postgres
  #   ports:
  #     - "10096:8080"
  #     - "9999:9990"
  #     - "8088:8443"
  #   volumes:
  #     - ./data:/data
  #   depends_on:
  #     - keycloak-db

  # keycloak-db:
  #   image: postgres:9.6.3
  #   env_file:
  #     - keycloak_default_env
  #     - keycloak_secrets

  ###########################
  ######## ELK Stack ########
  ###########################

#  elasticsearch:
#    build: elk/elasticsearch
#    ports:
#      - "9200:9200"
#      - "9300:9300"

  logstash:
    build: elk-prod/logstash/
    volumes:
      - ./elk-prod/logstash/log:/var/log/logstash
    ports:
      - "5000:5000"

  kibana:
    image: docker.elastic.docker-compose.yml/kibana/kibana:5.6.3
    environment:
      ELASTICSEARCH_URL: https://sl-eu-lon-2-portal.2.dblayer.com:21145
      ELASTICSEARCH_USERNAME: admin
      ELASTICSEARCH_PASSWORD: QTFQZOHIFHZKLIBC
      ELASTICSEARCH_SSL_VERIFY: "false"
      LOGGING_VERBOSE: "true"
    ports:
      - "5601:5601"